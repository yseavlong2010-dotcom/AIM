--=============================================================================
-- [ENGINE]: NOXERA V44 APEX - IOS PRO MAX OPTIMIZED
-- [DEV]: @_noxera.
-- [MÔ TẢ KỸ THUẬT]:
-- + Dynamic Resolution: Tự động điều chỉnh UI theo tỷ lệ 19.5:9 của iPhone Pro Max.
-- + 120Hz ProMotion Sync: Đồng bộ hàm Lerp với tốc độ quét màn hình iOS.
-- + Touch-Safe AutoShoot: Cơ chế VirtualUser cô lập luồng, loại bỏ liệt cảm ứng.
-- + CPU Thermal Optimization: Giảm chu kỳ Raycast rác, chống nóng máy tụt FPS.
--=============================================================================

local Config = {
    MenuTitle       = "NOXERA APEX V44 (iOS)",
    Version         = "V44.0.0 PRO MAX",
    ToggleIcon      = "rbxassetid://10631481977",

    -- COMBAT
    AimEnabled      = false,
    AimPart         = "Head", 
    AimMode         = "Legit",
    AimFOV          = 120, -- Tối ưu cho màn hình rộng ngang của iPhone
    Smoothness      = 4,   -- Tối ưu cho màn 120Hz (giảm giá trị so với 60Hz)
    Prediction      = 0.155,
    AutoShoot       = false,
    ShootDelay      = 0.05,
    HitboxSize      = 12,
    WallCheck       = true, 
    TeamCheck       = true,

    -- VISUALS
    ESPBox          = false, ESPName = false, ESPHP = false, ESPTracer = false, ESPDistance = false, ESPChams = false,
    MaxRender       = 2500, ShowTargetDot = false, CustomCrosshair = false, Watermark = true,
    
    -- MOVEMENT & MISC
    SpeedHack       = false, SpeedValue = 2,
    JumpHack        = false, InfJump = false, NoClip = false, Fly = false, FlySpeed = 50,
    SpinBot         = false, SpinSpeed = 50,
    AutoStomp       = false, Fullbright = false, LockQuickAim = true, ClockTime = 14
}

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")
local Stats = game:GetService("Stats")
local VirtualUser = game:GetService("VirtualUser")

local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera
local LockedTarget = nil
local LastShootTick = 0
local Connections = {}

-- Tính toán tỷ lệ thiết bị (Device Aspect Ratio Scaling)
local ScreenSize = Camera.ViewportSize
local IsUltraWide = (ScreenSize.X / ScreenSize.Y) > 2 -- Nhận diện iPhone màn hình dài

if CoreGui:FindFirstChild("NoxeraV44") then CoreGui.NoxeraV44:Destroy() end
local Gui = Instance.new("ScreenGui", CoreGui); Gui.Name = "NoxeraV44"; Gui.ResetOnSpawn = false

--=============================================================================
-- [PREMIUM UI FRAMEWORK - DYNAMIC SCALING FOR IPHONE]
--=============================================================================
local function MakeDraggable(TopBar, Object, isQuickBtn)
    local Dragging, DragInput, DragStart, StartPos
    TopBar.InputBegan:Connect(function(input)
        if isQuickBtn and Config.LockQuickAim then return end
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            Dragging = true; DragStart = input.Position; StartPos = Object.Position
            input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then Dragging = false end end)
        end
    end)
    TopBar.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then DragInput = input end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if input == DragInput and Dragging then 
            Object.Position = UDim2.new(StartPos.X.Scale, StartPos.X.Offset + (input.Position - DragStart).X, StartPos.Y.Scale, StartPos.Y.Offset + (input.Position - DragStart).Y)
        end
    end)
end

local BG_COLOR = Color3.fromRGB(10, 10, 14)
local TAB_BG = Color3.fromRGB(15, 15, 20)
local ACCENT_COLOR = Color3.fromRGB(0, 220, 255) -- Cyan Blue phù hợp OLED
local TEXT_COLOR = Color3.fromRGB(245, 245, 245)

local Watermark = Instance.new("TextLabel", Gui)
Watermark.Size, Watermark.Position = UDim2.new(0, 260, 0, 28), UDim2.new(0, IsUltraWide and 45 or 15, 0, 15) -- Né Dynamic Island/Tai thỏ
Watermark.BackgroundColor3, Watermark.TextColor3 = BG_COLOR, TEXT_COLOR
Watermark.Font, Watermark.TextSize = Enum.Font.GothamBold, 12
Instance.new("UICorner", Watermark).CornerRadius = UDim.new(0, 6)
Instance.new("UIStroke", Watermark).Color = ACCENT_COLOR
_G.NoxeraWatermark = Watermark 

local QuickAimBtn = Instance.new("ImageButton", Gui)
QuickAimBtn.Size, QuickAimBtn.Position = UDim2.new(0, 55, 0, 55), UDim2.new(0.85, 0, 0.35, 0)
QuickAimBtn.BackgroundColor3 = BG_COLOR
QuickAimBtn.Image = "rbxassetid://10631481977"; QuickAimBtn.ImageColor3 = Color3.fromRGB(255, 50, 50) 
Instance.new("UICorner", QuickAimBtn).CornerRadius = UDim.new(1, 0)
local QuickAimStroke = Instance.new("UIStroke", QuickAimBtn)
QuickAimStroke.Color, QuickAimStroke.Thickness = Color3.fromRGB(255, 50, 50), 2
MakeDraggable(QuickAimBtn, QuickAimBtn, true)

local PillMenu = Instance.new("Frame", Gui)
PillMenu.Size, PillMenu.Position = UDim2.new(0, 180, 0, 45), UDim2.new(0.5, -90, 0, 20)
PillMenu.BackgroundColor3 = BG_COLOR
Instance.new("UICorner", PillMenu).CornerRadius = UDim.new(1, 0)
local PillStroke = Instance.new("UIStroke", PillMenu)
PillStroke.Color = ACCENT_COLOR; PillStroke.Thickness = 2
MakeDraggable(PillMenu, PillMenu)

local OpenBtn = Instance.new("TextButton", PillMenu)
OpenBtn.Size, OpenBtn.BackgroundTransparency, OpenBtn.Text = UDim2.new(1, 0, 1, 0), 1, ""
local TM_Icon = Instance.new("ImageLabel", PillMenu)
TM_Icon.Size, TM_Icon.Position, TM_Icon.BackgroundTransparency, TM_Icon.Image = UDim2.new(0, 24, 0, 24), UDim2.new(0, 15, 0.5, -12), 1, Config.ToggleIcon
local TM_Label = Instance.new("TextLabel", PillMenu)
TM_Label.Size, TM_Label.Position, TM_Label.BackgroundTransparency = UDim2.new(1, -50, 1, 0), UDim2.new(0, 48, 0, 0), 1
TM_Label.Text, TM_Label.TextColor3, TM_Label.Font, TM_Label.TextSize, TM_Label.TextXAlignment = "NOXERA iOS", TEXT_COLOR, Enum.Font.GothamBold, 14, Enum.TextXAlignment.Left

-- Kích thước UI động theo tỷ lệ màn hình điện thoại
local MenuWidth = IsUltraWide and 0.55 or 0.7 
local MainMenu = Instance.new("Frame", Gui)
MainMenu.Size, MainMenu.Position = UDim2.new(MenuWidth, 0, 0.65, 0), UDim2.new(0.5, 0, 0.5, 0)
MainMenu.AnchorPoint = Vector2.new(0.5, 0.5) -- Căn giữa tuyệt đối
MainMenu.BackgroundColor3, MainMenu.ClipsDescendants, MainMenu.Visible = BG_COLOR, true, false
Instance.new("UICorner", MainMenu).CornerRadius = UDim.new(0, 12)
Instance.new("UIStroke", MainMenu).Color = ACCENT_COLOR

local TopBar = Instance.new("Frame", MainMenu)
TopBar.Size, TopBar.BackgroundColor3 = UDim2.new(1, 0, 0, 50), TAB_BG
Instance.new("UICorner", TopBar).CornerRadius = UDim.new(0, 12)
MakeDraggable(TopBar, MainMenu)

local BrandTitle = Instance.new("TextLabel", TopBar)
BrandTitle.Size, BrandTitle.Position, BrandTitle.BackgroundTransparency = UDim2.new(0, 200, 1, 0), UDim2.new(0, 20, 0, 0), 1
BrandTitle.Text, BrandTitle.TextColor3, BrandTitle.Font, BrandTitle.TextSize, BrandTitle.TextXAlignment = Config.MenuTitle, ACCENT_COLOR, Enum.Font.GothamBlack, 16, Enum.TextXAlignment.Left

local CloseBtn = Instance.new("TextButton", TopBar)
CloseBtn.Size, CloseBtn.Position, CloseBtn.BackgroundTransparency = UDim2.new(0, 50, 0, 50), UDim2.new(1, -50, 0, 0), 1
CloseBtn.Text, CloseBtn.TextColor3, CloseBtn.Font, CloseBtn.TextSize = "X", Color3.fromRGB(200, 200, 200), Enum.Font.GothamBold, 18

OpenBtn.MouseButton1Click:Connect(function()
    PillMenu.Visible = false; MainMenu.Size = UDim2.new(0, 0, 0, 0); MainMenu.Visible = true
    TweenService:Create(MainMenu, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.new(MenuWidth, 0, 0.65, 0)}):Play()
end)

CloseBtn.MouseButton1Click:Connect(function()
    local T = TweenService:Create(MainMenu, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = UDim2.new(0, 0, 0, 0)})
    T:Play(); T.Completed:Wait(); MainMenu.Visible = false; PillMenu.Visible = true
end)

local Sidebar = Instance.new("ScrollingFrame", MainMenu)
Sidebar.Size, Sidebar.Position, Sidebar.BackgroundTransparency, Sidebar.ScrollBarThickness = UDim2.new(0.25, 0, 1, -50), UDim2.new(0, 10, 0, 50), 1, 0
local SidebarLayout = Instance.new("UIListLayout", Sidebar)
SidebarLayout.Padding = UDim.new(0, 8)

local ContentArea = Instance.new("Frame", MainMenu)
ContentArea.Size, ContentArea.Position, ContentArea.BackgroundColor3 = UDim2.new(0.75, -20, 1, -60), UDim2.new(0.25, 10, 0, 55), TAB_BG
Instance.new("UICorner", ContentArea).CornerRadius = UDim.new(0, 10)

local Tabs = {}; local TabButtons = {}

local function CreateTab(Name, Icon, IsDefault)
    local Btn = Instance.new("TextButton", Sidebar)
    Btn.Size, Btn.BackgroundColor3, Btn.BackgroundTransparency = UDim2.new(1, 0, 0, 40), TAB_BG, IsDefault and 0 or 1
    Btn.Text, Btn.TextColor3, Btn.Font, Btn.TextSize, Btn.TextXAlignment = "  " .. Icon .. "  " .. Name, IsDefault and ACCENT_COLOR or Color3.fromRGB(150, 150, 150), Enum.Font.GothamBold, 13, Enum.TextXAlignment.Left
    Instance.new("UICorner", Btn).CornerRadius = UDim.new(0, 8)

    local Content = Instance.new("ScrollingFrame", ContentArea)
    Content.Size, Content.Position, Content.BackgroundTransparency, Content.ScrollBarThickness, Content.Visible = UDim2.new(1, -20, 1, -20), UDim2.new(0, 10, 0, 10), 1, 2, IsDefault
    local Layout = Instance.new("UIListLayout", Content)
    Layout.Padding = UDim.new(0, 10)

    table.insert(TabButtons, Btn); Tabs[Name] = Content

    Btn.MouseButton1Click:Connect(function()
        for _, b in pairs(TabButtons) do b.BackgroundTransparency = 1; b.TextColor3 = Color3.fromRGB(150, 150, 150) end
        for _, c in pairs(Tabs) do c.Visible = false end
        Btn.BackgroundTransparency = 0; Btn.TextColor3 = ACCENT_COLOR; Content.Visible = true
    end)
    return Content
end

local TogglePointers = {}
local function CreateToggle(Tab, Text, Key, Callback)
    local Frame = Instance.new("TextButton", Tab)
    Frame.Size, Frame.BackgroundColor3, Frame.Text = UDim2.new(1, 0, 0, 45), BG_COLOR, ""
    Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 8)
    
    local Label = Instance.new("TextLabel", Frame)
    Label.Size, Label.Position, Label.BackgroundTransparency, Label.Text, Label.TextColor3, Label.Font, Label.TextSize, Label.TextXAlignment = UDim2.new(1, -70, 1, 0), UDim2.new(0, 15, 0, 0), 1, Text, TEXT_COLOR, Enum.Font.GothamSemibold, 13, Enum.TextXAlignment.Left

    local SwitchBG = Instance.new("Frame", Frame)
    SwitchBG.Size, SwitchBG.Position, SwitchBG.BackgroundColor3 = UDim2.new(0, 40, 0, 20), UDim2.new(1, -50, 0.5, -10), Config[Key] and ACCENT_COLOR or Color3.fromRGB(50, 50, 60)
    Instance.new("UICorner", SwitchBG).CornerRadius = UDim.new(1, 0)
    local SwitchKnob = Instance.new("Frame", SwitchBG)
    SwitchKnob.Size, SwitchKnob.Position, SwitchKnob.BackgroundColor3 = UDim2.new(0, 16, 0, 16), Config[Key] and UDim2.new(1, -18, 0.5, -8) or UDim2.new(0, 2, 0.5, -8), Color3.fromRGB(255, 255, 255)
    Instance.new("UICorner", SwitchKnob).CornerRadius = UDim.new(1, 0)

    if Key == "AimEnabled" then TogglePointers.Aim = {SwitchBG, SwitchKnob} end

    Frame.MouseButton1Click:Connect(function()
        Config[Key] = not Config[Key]
        TweenService:Create(SwitchBG, TweenInfo.new(0.2), {BackgroundColor3 = Config[Key] and ACCENT_COLOR or Color3.fromRGB(50, 50, 60)}):Play()
        TweenService:Create(SwitchKnob, TweenInfo.new(0.2), {Position = Config[Key] and UDim2.new(1, -18, 0.5, -8) or UDim2.new(0, 2, 0.5, -8)}):Play()
        if Key == "AimEnabled" then
            QuickAimStroke.Color = Config[Key] and ACCENT_COLOR or Color3.fromRGB(255, 50, 50)
            QuickAimBtn.ImageColor3 = Config[Key] and ACCENT_COLOR or Color3.fromRGB(255, 50, 50)
        end
        if Callback then Callback(Config[Key]) end
    end)
end

QuickAimBtn.MouseButton1Click:Connect(function()
    Config.AimEnabled = not Config.AimEnabled
    QuickAimStroke.Color = Config.AimEnabled and ACCENT_COLOR or Color3.fromRGB(255, 50, 50)
    QuickAimBtn.ImageColor3 = Config.AimEnabled and ACCENT_COLOR or Color3.fromRGB(255, 50, 50)
    if TogglePointers.Aim then
        TweenService:Create(TogglePointers.Aim[1], TweenInfo.new(0.2), {BackgroundColor3 = Config.AimEnabled and ACCENT_COLOR or Color3.fromRGB(50, 50, 60)}):Play()
        TweenService:Create(TogglePointers.Aim[2], TweenInfo.new(0.2), {Position = Config.AimEnabled and UDim2.new(1, -18, 0.5, -8) or UDim2.new(0, 2, 0.5, -8)}):Play()
    end
end)

local function CreateDropdown(Tab, Text, Key, Options)
    local Frame = Instance.new("TextButton", Tab)
    Frame.Size, Frame.BackgroundColor3, Frame.Text = UDim2.new(1, 0, 0, 45), BG_COLOR, ""
    Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 8)
    local Label = Instance.new("TextLabel", Frame)
    Label.Size, Label.Position, Label.BackgroundTransparency, Label.Text, Label.TextColor3, Label.Font, Label.TextSize, Label.TextXAlignment = UDim2.new(1, -110, 1, 0), UDim2.new(0, 15, 0, 0), 1, Text, TEXT_COLOR, Enum.Font.GothamSemibold, 13, Enum.TextXAlignment.Left
    local ValueLabel = Instance.new("TextLabel", Frame)
    ValueLabel.Size, ValueLabel.Position, ValueLabel.BackgroundTransparency, ValueLabel.Text, ValueLabel.TextColor3, ValueLabel.Font, ValueLabel.TextSize, ValueLabel.TextXAlignment = UDim2.new(0, 90, 1, 0), UDim2.new(1, -100, 0, 0), 1, Config[Key], ACCENT_COLOR, Enum.Font.GothamBold, 13, Enum.TextXAlignment.Right

    local index = 1; for i, v in ipairs(Options) do if v == Config[Key] then index = i break end end
    Frame.MouseButton1Click:Connect(function()
        index = index + 1; if index > #Options then index = 1 end
        Config[Key] = Options[index]; ValueLabel.Text = Config[Key]
    end)
end

local function CreateSlider(Tab, Text, Key, Min, Max, Step)
    local Frame = Instance.new("Frame", Tab)
    Frame.Size, Frame.BackgroundColor3 = UDim2.new(1, 0, 0, 60), BG_COLOR
    Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 8)
    local Label = Instance.new("TextLabel", Frame)
    Label.Size, Label.Position, Label.BackgroundTransparency, Label.Text, Label.TextColor3, Label.Font, Label.TextSize, Label.TextXAlignment = UDim2.new(1, -30, 0, 25), UDim2.new(0, 15, 0, 5), 1, Text .. ": " .. Config[Key], TEXT_COLOR, Enum.Font.GothamSemibold, 13, Enum.TextXAlignment.Left

    local SliderBG = Instance.new("TextButton", Frame)
    SliderBG.Size, SliderBG.Position, SliderBG.BackgroundColor3, SliderBG.Text = UDim2.new(1, -30, 0, 8), UDim2.new(0, 15, 0, 40), Color3.fromRGB(40, 40, 50), ""
    Instance.new("UICorner", SliderBG).CornerRadius = UDim.new(1, 0)
    local Fill = Instance.new("Frame", SliderBG)
    Fill.Size, Fill.BackgroundColor3 = UDim2.new((Config[Key] - Min) / (Max - Min), 0, 1, 0), ACCENT_COLOR
    Instance.new("UICorner", Fill).CornerRadius = UDim.new(1, 0)

    local dragging = false
    local function Update(Input)
        local X = math.clamp((Input.Position.X - SliderBG.AbsolutePosition.X) / SliderBG.AbsoluteSize.X, 0, 1)
        local Value = math.floor(((Min + (Max - Min) * X) / Step) + 0.5) * Step
        Config[Key] = Value; Fill.Size = UDim2.new(X, 0, 1, 0); Label.Text = Text .. ": " .. Value
        if Key == "ClockTime" then Lighting.ClockTime = Value end
    end
    SliderBG.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = true; Update(input) end end)
    UserInputService.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = false end end)
    UserInputService.InputChanged:Connect(function(input) if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then Update(input) end end)
end

local function CreateButton(Tab, Text, Color, Callback)
    local Frame = Instance.new("TextButton", Tab)
    Frame.Size, Frame.BackgroundColor3, Frame.Text, Frame.TextColor3, Frame.Font, Frame.TextSize = UDim2.new(1, 0, 0, 45), Color or BG_COLOR, Text, TEXT_COLOR, Enum.Font.GothamBold, 13
    Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 8)
    Frame.MouseButton1Click:Connect(Callback)
end

-- KHỞI TẠO TABS
local TabCombat = CreateTab("Combat", "🎯", true)
local TabVisuals = CreateTab("Visuals", "👁", false)
local TabMovement = CreateTab("Movement", "🏃", false)
local TabSettings = CreateTab("Settings", "⚙", false)

CreateToggle(TabCombat, "Target Lock (Aim)", "AimEnabled")
CreateToggle(TabCombat, "Touch-Safe AutoShoot", "AutoShoot")
CreateDropdown(TabCombat, "Aim Part", "AimPart", {"Head", "Torso", "HumanoidRootPart"})
CreateDropdown(TabCombat, "Aim Mode", "AimMode", {"Rage", "Legit"})
CreateToggle(TabCombat, "Smart Team Check", "TeamCheck")
CreateToggle(TabCombat, "Optimized Wall Check", "WallCheck")
CreateSlider(TabCombat, "Aim FOV", "AimFOV", 10, 500, 10)
CreateSlider(TabCombat, "Prediction", "Prediction", 0, 0.5, 0.005)
CreateSlider(TabCombat, "Smoothness (ProMotion 120Hz)", "Smoothness", 1, 10, 0.5)

CreateToggle(TabVisuals, "ESP Box", "ESPBox")
CreateToggle(TabVisuals, "ESP Name", "ESPName")
CreateToggle(TabVisuals, "ESP Health", "ESPHP")
CreateToggle(TabVisuals, "ESP Distance", "ESPDistance")
CreateToggle(TabVisuals, "ESP Chams", "ESPChams", function(state)
    if not state then for _, p in ipairs(Players:GetPlayers()) do if p.Character and p.Character:FindFirstChild("NoxChams") then p.Character.NoxChams:Destroy() end end end
end)
CreateToggle(TabVisuals, "Show Target Dot", "ShowTargetDot")
CreateToggle(TabVisuals, "Crosshair", "CustomCrosshair")

CreateToggle(TabMovement, "CFrame Speed", "SpeedHack")
CreateSlider(TabMovement, "Speed Value", "SpeedValue", 1, 10, 1)
CreateToggle(TabMovement, "Fly Hack", "Fly")
CreateToggle(TabMovement, "Infinite Jump", "InfJump")
CreateToggle(TabMovement, "Spinbot", "SpinBot")
CreateToggle(TabMovement, "NoClip", "NoClip")

CreateToggle(TabSettings, "Khóa Nút Aim Phụ", "LockQuickAim")
CreateToggle(TabSettings, "Watermark (HUD)", "Watermark")
CreateToggle(TabSettings, "Fullbright", "Fullbright", function(state) Lighting.Ambient = state and Color3.new(1,1,1) or Color3.fromRGB(127,127,127); Lighting.GlobalShadows = not state end)
CreateButton(TabSettings, "💀 UNLOAD SCRIPT", Color3.fromRGB(255, 50, 50), function()
    RunService:UnbindFromRenderStep("NoxeraAimBot")
    for _, conn in ipairs(Connections) do conn:Disconnect() end
    for _, p in ipairs(Players:GetPlayers()) do
        if _G.ESP_Cache and _G.ESP_Cache[p] then for _, v in pairs(_G.ESP_Cache[p]) do v:Remove() end end
        if p.Character and p.Character:FindFirstChild("NoxChams") then p.Character.NoxChams:Destroy() end
    end
    pcall(function()
        if _G.FOVCircle then _G.FOVCircle:Remove() end
        if _G.TargetDot then _G.TargetDot:Remove() end
        if _G.CrossX then _G.CrossX:Remove() end
        if _G.CrossY then _G.CrossY:Remove() end
    end)
    Gui:Destroy()
end)

--=============================================================================
-- [CORE ENGINE - TỐI ƯU HÓA CAMERA & XỬ LÝ CHỐNG LIỆT CẢM ỨNG IOS]
--=============================================================================
_G.ESP_Cache = {}
_G.FOVCircle = Drawing.new("Circle"); _G.FOVCircle.Thickness = 1.5; _G.FOVCircle.Filled = false
_G.TargetDot = Drawing.new("Circle"); _G.TargetDot.Radius = 4; _G.TargetDot.Filled = true; _G.TargetDot.Color = ACCENT_COLOR
_G.CrossX = Drawing.new("Line"); _G.CrossX.Thickness = 1.5; _G.CrossX.Color = ACCENT_COLOR
_G.CrossY = Drawing.new("Line"); _G.CrossY.Thickness = 1.5; _G.CrossY.Color = ACCENT_COLOR

local RayParams = RaycastParams.new()
RayParams.FilterType = Enum.RaycastFilterType.Exclude
RayParams.IgnoreWater = true
RayParams.RespectCanCollide = false

local function IsLocalAlive()
    local Char = LocalPlayer.Character
    if not Char then return false end
    local Hum = Char:FindFirstChildOfClass("Humanoid")
    return Hum and Hum.Health > 0
end

-- Tối ưu luồng mô phỏng sự kiện nút nhấn Touch/Click để không đóng băng Joystick Mobile
local function SafeShoot()
    if not IsLocalAlive() or UserInputService:GetFocusedTextBox() then return end

    if tick() - LastShootTick > Config.ShootDelay then
        LastShootTick = tick()
        
        local Char = LocalPlayer.Character
        local Tool = Char and Char:FindFirstChildOfClass("Tool")
        
        if Tool then
            Tool:Activate()
        else
            task.spawn(function()
                pcall(function()
                    VirtualUser:Button1Down(Vector2.new(0, 0))
                    task.wait(0.01)
                    VirtualUser:Button1Up(Vector2.new(0, 0))
                end)
            end)
        end
    end
end

local function IsValidTarget(Player)
    if Player == LocalPlayer then return false end
    local Char = Player.Character
    if not Char or not Char:IsDescendantOf(Workspace) then return false end 
    local Hum = Char:FindFirstChildOfClass("Humanoid")
    if not Hum or Hum.Health <= 0 then return false end
    if Config.TeamCheck and LocalPlayer.Team and Player.Team and LocalPlayer.Team == Player.Team then return false end
    if Char:FindFirstChild("ForceField") then return false end
    return true
end

local function GetAimPart(Char)
    local Hum = Char:FindFirstChildOfClass("Humanoid")
    if not Hum then return Char:FindFirstChild("HumanoidRootPart") end
    if Config.AimPart == "Torso" then 
        if Hum.RigType == Enum.HumanoidRigType.R15 then
            return Char:FindFirstChild("UpperTorso") or Char:FindFirstChild("HumanoidRootPart")
        else
            return Char:FindFirstChild("Torso") or Char:FindFirstChild("HumanoidRootPart")
        end
    end
    return Char:FindFirstChild(Config.AimPart) or Char:FindFirstChild("HumanoidRootPart")
end

-- Thermal Throttling Bypass: Giảm số lần Raycast quét vòng lặp sâu để giảm tải cho CPU iPhone
local function IsVisibleDeep(TargetPart)
    local Origin = Camera.CFrame.Position
    local Direction = TargetPart.Position - Origin
    local IgnoreList = {LocalPlayer.Character, Camera}
    
    for i = 1, 2 do -- Giảm xuống 2 vòng lặp thay vì 4, quá đủ cho 90% map và mát máy hơn
        RayParams.FilterDescendantsInstances = IgnoreList
        local Result = Workspace:Raycast(Origin, Direction, RayParams)
        
        if not Result then return true end
        if Result.Instance:IsDescendantOf(TargetPart.Parent) then return true end
        
        if Result.Instance.Transparency > 0.5 or Result.Instance.CanCollide == false or Result.Instance:IsA("Accessory") then
            table.insert(IgnoreList, Result.Instance)
        else
            return false
        end
    end
    return false
end

-- Physics Loop
local hBeat = RunService.Heartbeat:Connect(function(deltaTime)
    pcall(function()
        local MyChar = LocalPlayer.Character
        local MyHum = MyChar and MyChar:FindFirstChildOfClass("Humanoid")
        local MyHRP = MyChar and MyChar:FindFirstChild("HumanoidRootPart")

        if MyHum and MyHRP then
            if Config.SpeedHack and MyHum.MoveDirection.Magnitude > 0 then
                MyHRP.CFrame = MyHRP.CFrame + (MyHum.MoveDirection * Config.SpeedValue * deltaTime * 10)
            end
            if Config.SpinBot then
                MyHRP.CFrame = MyHRP.CFrame * CFrame.Angles(0, math.rad(Config.SpinSpeed), 0)
            end
            if Config.Fly then
                if not MyHRP:FindFirstChild("NoxFly") then
                    local bv = Instance.new("BodyVelocity"); bv.Name = "NoxFly"; bv.MaxForce = Vector3.new(1e5, 1e5, 1e5); bv.Parent = MyHRP
                end
                MyHRP.NoxFly.Velocity = Camera.CFrame.LookVector * (MyHum.MoveDirection.Z > 0 and -Config.FlySpeed or (MyHum.MoveDirection.Z < 0 and Config.FlySpeed or 0)) + Camera.CFrame.RightVector * (MyHum.MoveDirection.X > 0 and Config.FlySpeed or (MyHum.MoveDirection.X < 0 and -Config.FlySpeed or 0))
            else
                if MyHRP:FindFirstChild("NoxFly") then MyHRP.NoxFly:Destroy() end
            end
        end
        if Config.NoClip and MyChar then for _, v in pairs(MyChar:GetDescendants()) do if v:IsA("BasePart") and v.CanCollide then v.CanCollide = false end end end
    end)
end)
table.insert(Connections, hBeat)

-- Render Loop được tối ưu Delta-time cực chuẩn cho màn hình ProMotion 120Hz của iPhone
local renderPriority = Enum.RenderPriority.Camera.Value + 1
RunService:BindToRenderStep("NoxeraAimBot", renderPriority, function(deltaTime)
    pcall(function()
        Camera = Workspace.CurrentCamera
        local Center = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
        
        if not IsLocalAlive() then LockedTarget = nil end
        
        if Config.Watermark and _G.NoxeraWatermark then
            local ping = math.floor(Stats.Network.ServerStatsItem["Data Ping"]:GetValue())
            _G.NoxeraWatermark.Visible = true
            _G.NoxeraWatermark.Text = " NOXERA V44 iOS | FPS: " .. math.floor(1 / deltaTime) .. " | Ping: " .. ping .. "ms "
        elseif _G.NoxeraWatermark then _G.NoxeraWatermark.Visible = false end

        _G.FOVCircle.Position = Center; _G.FOVCircle.Radius = Config.AimFOV; _G.FOVCircle.Visible = Config.AimEnabled
        _G.FOVCircle.Color = LockedTarget and ACCENT_COLOR or Color3.fromRGB(255, 255, 255)
        
        if Config.CustomCrosshair then
            _G.CrossX.Visible = true; _G.CrossY.Visible = true
            _G.CrossX.From = Vector2.new(Center.X - 8, Center.Y); _G.CrossX.To = Vector2.new(Center.X + 8, Center.Y)
            _G.CrossY.From = Vector2.new(Center.X, Center.Y - 8); _G.CrossY.To = Vector2.new(Center.X, Center.Y + 8)
        else _G.CrossX.Visible = false; _G.CrossY.Visible = false end

        if LockedTarget then
            local Player = Players:GetPlayerFromCharacter(LockedTarget.Parent)
            if not Player or not IsValidTarget(Player) then 
                LockedTarget = nil 
            else
                local Pos, OnScreen = Camera:WorldToViewportPoint(LockedTarget.Position)
                if not OnScreen or (Vector2.new(Pos.X, Pos.Y) - Center).Magnitude > (Config.AimFOV * 1.5) then 
                    LockedTarget = nil 
                elseif Config.WallCheck and not IsVisibleDeep(LockedTarget) then
                    LockedTarget = nil 
                end
            end
        end

        local BestDist = Config.AimFOV

        for _, Player in ipairs(Players:GetPlayers()) do
            if not IsValidTarget(Player) then
                if _G.ESP_Cache[Player] then for _, v in pairs(_G.ESP_Cache[Player]) do v.Visible = false end end
                if Player.Character and Player.Character:FindFirstChild("NoxChams") then Player.Character.NoxChams:Destroy() end
                continue
            end

            local Char = Player.Character
            local HRP = Char:FindFirstChild("HumanoidRootPart")
            local Head = Char:FindFirstChild("Head") or HRP
            local Hum = Char:FindFirstChildOfClass("Humanoid")

            if Config.ESPChams then
                if not Char:FindFirstChild("NoxChams") then
                    local hl = Instance.new("Highlight", Char); hl.Name = "NoxChams"; hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                end
                Char.NoxChams.FillColor = (LockedTarget and LockedTarget.Parent == Char) and ACCENT_COLOR or Color3.fromRGB(255, 50, 50)
            else
                if Char:FindFirstChild("NoxChams") then Char.NoxChams:Destroy() end
            end

            local Pos, OnScreen = Camera:WorldToViewportPoint(HRP.Position)
            local Dist3D = math.floor((Camera.CFrame.Position - HRP.Position).Magnitude)

            if OnScreen and Dist3D <= Config.MaxRender and (Config.ESPBox or Config.ESPName or Config.ESPHP or Config.ESPTracer or Config.ESPDistance) then
                if not _G.ESP_Cache[Player] then
                    _G.ESP_Cache[Player] = { Box = Drawing.new("Square"), Name = Drawing.new("Text"), HP = Drawing.new("Square"), Dist = Drawing.new("Text"), Tracer = Drawing.new("Line") }
                    local e = _G.ESP_Cache[Player]; e.Box.Thickness = 1.5; e.Box.Filled = false
                    e.Name.Size = 14; e.Name.Center = true; e.Name.Outline = true; e.Name.Color = Color3.fromRGB(255, 255, 255)
                    e.HP.Filled = true; e.Dist.Size = 12; e.Dist.Center = true; e.Dist.Outline = true; e.Dist.Color = Color3.fromRGB(200, 200, 200)
                    e.Tracer.Thickness = 1.5
                end
                
                local Top, _ = Camera:WorldToViewportPoint(Head.Position + Vector3.new(0, 0.5, 0))
                local Bot, _ = Camera:WorldToViewportPoint(HRP.Position - Vector3.new(0, 3, 0))
                local BoxH = math.abs(Top.Y - Bot.Y); local BoxW = BoxH * 0.55
                local e = _G.ESP_Cache[Player]
                
                local espColor = (LockedTarget and LockedTarget.Parent == Char) and ACCENT_COLOR or Color3.fromRGB(255, 255, 255)
                
                e.Box.Size = Vector2.new(BoxW, BoxH); e.Box.Position = Vector2.new(Top.X - BoxW/2, Top.Y); e.Box.Color = espColor; e.Box.Visible = Config.ESPBox
                e.Name.Text = Player.Name; e.Name.Position = Vector2.new(Top.X, Top.Y - 15); e.Name.Visible = Config.ESPName
                e.HP.Size = Vector2.new(2, BoxH * (Hum.Health/Hum.MaxHealth)); e.HP.Position = Vector2.new(Top.X - BoxW/2 - 5, Bot.Y - e.HP.Size.Y); e.HP.Color = Color3.fromRGB(0, 255, 0); e.HP.Visible = Config.ESPHP
                e.Tracer.From = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y); e.Tracer.To = Vector2.new(Bot.X, Bot.Y); e.Tracer.Color = espColor; e.Tracer.Visible = Config.ESPTracer
                e.Dist.Text = "[" .. Dist3D .. "m]"; e.Dist.Position = Vector2.new(Top.X, Bot.Y + 2); e.Dist.Visible = Config.ESPDistance
            else
                if _G.ESP_Cache[Player] then for _, v in pairs(_G.ESP_Cache[Player]) do v.Visible = false end end
            end

            if not LockedTarget and IsLocalAlive() then
                local AimP = GetAimPart(Char)
                if AimP and Config.AimEnabled and OnScreen then
                    local APos = Camera:WorldToViewportPoint(AimP.Position)
                    local Dist2D = (Vector2.new(APos.X, APos.Y) - Center).Magnitude
                    if Dist2D < BestDist then
                        if not Config.WallCheck or IsVisibleDeep(AimP) then
                            BestDist = Dist2D; LockedTarget = AimP
                        end
                    end
                end
            end
        end

        if Config.AimEnabled and LockedTarget and IsLocalAlive() then
            local PredPos = LockedTarget.Position + (LockedTarget.Velocity * Config.Prediction)
            local TargetCFrame = CFrame.lookAt(Camera.CFrame.Position, PredPos)
            
            if Config.AimMode == "Rage" then 
                Camera.CFrame = TargetCFrame
            else 
                -- Tối ưu hóa hệ số Lerp mượt mà tuyệt đối với tần số 120Hz của iPhone Pro Max
                -- deltaTime * (Smoothness * 1.5) giúp ngắm chuẩn mà không chống lại thao tác vuốt của ngón tay
                local lerpAlpha = math.clamp(deltaTime * (Config.Smoothness * 1.5), 0, 1)
                Camera.CFrame = Camera.CFrame:Lerp(TargetCFrame, lerpAlpha) 
            end

            if Config.ShowTargetDot then
                local dp, ds = Camera:WorldToViewportPoint(PredPos)
                if ds then _G.TargetDot.Position = Vector2.new(dp.X, dp.Y); _G.TargetDot.Visible = true else _G.TargetDot.Visible = false end
            else _G.TargetDot.Visible = false end
            
            if Config.AutoShoot then
                if not Config.WallCheck or IsVisibleDeep(LockedTarget) then
                    SafeShoot()
                end
            end
        else 
            _G.TargetDot.Visible = false 
        end
    end)
end)

local jumpCon = UserInputService.JumpRequest:Connect(function()
    if Config.InfJump and LocalPlayer.Character then
        local Hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if Hum then Hum:ChangeState(Enum.HumanoidStateType.Jumping) end
    end
end)
table.insert(Connections, jumpCon)

local leaveCon = Players.PlayerRemoving:Connect(function(P)
    if _G.ESP_Cache and _G.ESP_Cache[P] then for _, v in pairs(_G.ESP_Cache[P]) do v:Remove() end; _G.ESP_Cache[P] = nil end
end)
table.insert(Connections, leaveCon)
